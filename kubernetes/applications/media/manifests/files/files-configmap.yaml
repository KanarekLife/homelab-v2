kind: ConfigMap
apiVersion: v1
metadata:
  name: files-configmap
  namespace: media
data:
  config.yaml: |
    server:
      sources:
        - path: /media
          name: Media
        - path: /storage
          name: Storage
        - path: /suwayomi-files
          name: Suwayomi Files
        - path: /suwayomi-backup-files
          name: Suwayomi Backup Files
      cacheDir: /cache
    auth:
      methods:
        password:
          enabled: false
        oidc:
          enabled: true
          issuerUrl: https://login.nieradko.com/application/o/files/
          scopes: "email openid profile groups"
          userIdentifier: "preferred_username"
          createUser: true
          adminGroup: "Files Admins"
          groupsClaim: "groups"

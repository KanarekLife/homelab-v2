# Recommend-only VPA (updateMode: Off). Read: kubectl describe vpa -n media suwayomi-server
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: suwayomi-server
  namespace: media
spec:
  targetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: suwayomi-server-deployment
  updatePolicy:
    updateMode: "Off"

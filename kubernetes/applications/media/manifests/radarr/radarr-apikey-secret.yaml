apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: radarr-apikey-secret
  namespace: media
  annotations:
    argocd.argoproj.io/sync-options: Prune=true
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: bitwarden-secretsmanager
    kind: ClusterSecretStore
  data:
    - secretKey: apikey
      remoteRef:
        key: "9327431a-c04c-4774-bf1d-b39000fc3a45"
